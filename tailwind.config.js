const { color } = require("motion");

module.exports = {
  theme: {
    extend: {
      fontFamily: {
        switzer: ["var(--font-switzer)", "sans-serif"],
      },
      colors: {
        black: "#000000",
        voxel: "#A7A7A7",
        voxgred: "#1D1D1D",
        backvox:"#111111"
      },
    },
  },
};
